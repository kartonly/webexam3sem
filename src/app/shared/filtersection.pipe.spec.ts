import { FiltersectionPipe } from './filtersection.pipe';

describe('FiltersectionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltersectionPipe();
    expect(pipe).toBeTruthy();
  });
});
