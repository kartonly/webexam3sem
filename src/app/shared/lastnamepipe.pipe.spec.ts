import { LastnamepipePipe } from './lastnamepipe.pipe';

describe('LastnamepipePipe', () => {
  it('create an instance', () => {
    const pipe = new LastnamepipePipe();
    expect(pipe).toBeTruthy();
  });
});
