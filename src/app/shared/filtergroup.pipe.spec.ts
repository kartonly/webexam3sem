import { FiltergroupPipe } from './filtergroup.pipe';

describe('FiltergroupPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltergroupPipe();
    expect(pipe).toBeTruthy();
  });
});
